[package]
name = "blrs-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
blrs = { path = "../blrs", features = ["gzip", "zstd"] }
clap = { version = "4.5.17", features = ["derive"] }
figment = "0.10.19"
